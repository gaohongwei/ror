bundle 
bundle update abc

# Install gems from local directory
gem "my_common", '0.1.0', path: "./gems"
gem 'pry', path: './pry'


You can install gems into a specific folder (example ./vendor/):
bundle install --path vendor
