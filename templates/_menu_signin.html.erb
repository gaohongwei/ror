
        <%############## Hard coded menu,singup/singin/logout ##############%>
        <% if user_signed_in? %>   
        <ul class="nav navbar-nav">        
          <li class="dropdown">
            <%= link_to("#{current_user.vname} <b class='caret'></b>".html_safe, "#", {class: "dropdown-toggle", 'data-toggle' => "dropdown"}) %>
            <ul class="dropdown-menu">
              <li><%= link_to(tt('Logout'), destroy_user_session_path, :method => :delete) %></li> 
            </ul>
          </li>
        </ul>          
        <% else %>
        <ul class="nav navbar-nav">  
            <li class="dropdown"><%= link_to tt("login"), new_user_session_path %> </li>     
        </ul> 
        <ul class="nav navbar-nav">  
            <li class="dropdown"><%= link_to tt("sign_up"), new_user_registration_path %></li>       
        </ul>            
        <% end %>                

        <%############## Hard coded menu ##############%>
